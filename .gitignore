## macOS
.DS_Store

## Swift Package Manager
.build/
.swiftpm/
# Package.resolved is committed on purpose: xscaffold ships as an executable,
# and pinning its dependency graph is the same reproducibility promise the tool
# makes to its own users.

## Xcode
DerivedData/
*.xcodeproj
*.xcworkspace
xcuserdata/

## Local tooling state
.omc/
